# ML research pipeline + live meta-filter (optional — the core app runs without these).
#   pip install -r requirements-ml.txt
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3      # HistGradientBoostingClassifier (no native deps, beginner-friendly)
scipy>=1.10            # deflated Sharpe ratio math
joblib>=1.3            # save/load trained model bundles
databento>=0.40        # historical CME data fetch (research/fetch_databento.py)

# Optional stronger boosters (need libomp on macOS: `brew install libomp`):
# lightgbm>=4.0
# xgboost>=2.0
